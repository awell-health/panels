import { describe, it, expect } from 'vitest'

describe('Sample bot test', () => {
  it('This is where you can write tests for the functions used in bots', () => {
    expect(true).toBe(true)
  })
})
